# Frozen requirements for Toyota LiveKit Agent Project
# Generated based on actual package usage in the codebase

# LiveKit Agent Framework and Plugins
livekit-agents==1.2.6
livekit-plugins-azure==1.2.5
livekit-plugins-elevenlabs==1.2.5
livekit-plugins-noise-cancellation==0.2.5
livekit-plugins-openai==1.2.5
livekit-plugins-silero==1.2.5
livekit==1.0.12
livekit-api==1.0.5
livekit-protocol==1.0.4

# Core dependencies for environment and HTTP requests
python-dotenv==1.1.1
requests==2.32.4

# Required transitive dependencies for LiveKit
av==15.0.0
numpy==1.26.4
sounddevice==0.5.2

# Azure Speech Services (required by livekit-plugins-azure)
azure-cognitiveservices-speech==1.45.0
azure-core==1.35.0

# OpenAI integration (required by livekit-plugins-openai)
openai==1.99.6

# HTTP and async support
aiohttp==3.11.11
httpx==0.27.0

# Audio processing dependencies
sounddevice==0.5.2

# Standard utility packages
typing-extensions==4.12.2
